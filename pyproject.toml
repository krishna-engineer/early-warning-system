[project]
name = "detention-prevention"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "pandas>=3.0.5",
    "scipy>=1.17.1",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
